---
title: Middleware
description: ใช้ built-in middleware ของ Echo (RequestLogger, Recover, CORS) และเขียน middleware เอง — เปรียบกับ Express middleware
---

import TsGo from '../../../../components/TsGo.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import GoOnly from '../../../../components/GoOnly.astro';

## เปรียบเทียบโมเดล middleware

Express middleware คือ `(req, res, next) => void` เรียก `next()` เพื่อส่งต่อ หรือเรียก `res.json()` เพื่อจบ request เลย ส่วน Echo ใช้แนวคิด pipeline เดียวกันแต่อาศัย error-return pattern และ closure ของ Go:

```go
// Echo middleware signature
func(next echo.HandlerFunc) echo.HandlerFunc
```

Middleware คืน `HandlerFunc` ใหม่ที่ wrap `next` ไว้ เรียก `next(c)` เพื่อส่งต่อ หรือคืน error เพื่อยกเลิก

export const tsMiddleware = `// Express middleware
function requestLogger(req, res, next) {
  console.log(req.method + " " + req.path);
  next();
}
app.use(requestLogger);`;

export const goMiddleware = `// Echo middleware
func RequestLogger(next echo.HandlerFunc) echo.HandlerFunc {
    return func(c *echo.Context) error {
        fmt.Printf("%s %s\\n", c.Request().Method, c.Request().URL.Path)
        return next(c)   // pass control forward
    }
}

e.Use(RequestLogger)`;

<TsGo ts={tsMiddleware} go={goMiddleware} />

## Built-in middleware

Echo มี middleware พร้อมใช้งานใน production หลายตัวใน sub-package `middleware`

export const tsBuiltIn = `// Express with popular packages
import morgan from 'morgan';
import cors from 'cors';

app.use(morgan('combined'));
app.use(cors({ origin: '*' }));
// No built-in recover — need express-async-errors or similar`;

export const goBuiltIn = `// Echo built-ins — no extra packages needed
import "github.com/labstack/echo/v5/middleware"

e.Use(middleware.RequestLogger())  // request log via log/slog
e.Use(middleware.Recover())        // catches panics, returns 500
e.Use(middleware.CORS())           // default: allows all origins

// Restrict CORS to specific origins:
e.Use(middleware.CORSWithConfig(middleware.CORSConfig{
    AllowOrigins: []string{"https://example.com"},
    AllowMethods: []string{http.MethodGet, http.MethodPost},
}))`;

<TsGo ts={tsBuiltIn} go={goBuiltIn} />

Echo v5 log ด้วย `log/slog` ของ standard library — ไม่มี logger interface ของตัวเองอีกแล้ว `RequestLogger()` ใช้ `e.Logger` (เป็น `*slog.Logger`) ถ้าอยากเลือก field เอง ใช้ `RequestLoggerWithConfig` แล้ว log ค่าเองแบบนี้:

```go
e.Use(middleware.RequestLoggerWithConfig(middleware.RequestLoggerConfig{
    LogStatus: true,
    LogURI:    true,
    LogValuesFunc: func(c *echo.Context, v middleware.RequestLoggerValues) error {
        slog.Info("request", "uri", v.URI, "status", v.Status)
        return nil
    },
}))
```

## ลำดับ middleware

เหมือน Express ลำดับมีความสำคัญ middleware ที่ลงทะเบียนก่อนจะทำงานก่อน RequestLogger และ Recover ควรอยู่ชั้นนอกสุดเสมอ

```go
e.Use(middleware.RequestLogger())  // 1st — log all requests
e.Use(middleware.Recover())        // 2nd — catch panics from any later handler
e.Use(authMiddleware)              // 3rd — auth checks after logging is set up
```

## Middleware ระดับ route และ group

ใน Express คุณส่ง middleware เข้าไปที่ route ได้ตรง ๆ ส่วน Echo ทำแบบเดียวกันได้ด้วย `.Use` บน group

export const tsRouteMiddleware = `// Express — per-route middleware
app.get('/admin', authMiddleware, adminHandler);

// Express — router-level
const adminRouter = express.Router();
adminRouter.use(authMiddleware);
adminRouter.get('/', adminHandler);
app.use('/admin', adminRouter);`;

export const goRouteMiddleware = `// Echo — per-route middleware
e.GET("/admin", adminHandler, authMiddleware)

// Echo — group-level middleware
admin := e.Group("/admin")
admin.Use(authMiddleware)
admin.GET("", adminHandler)
admin.GET("/users", adminUsersHandler)`;

<TsGo ts={tsRouteMiddleware} go={goRouteMiddleware} />

## เขียน auth middleware เอง

export const tsAuthMiddleware = `// Express JWT-like auth middleware
function requireAuth(req, res, next) {
  const token = req.headers.authorization?.split(' ')[1];
  if (!token) return res.status(401).json({ message: 'unauthorized' });
  try {
    req.user = verifyToken(token);
    next();
  } catch {
    res.status(401).json({ message: 'invalid token' });
  }
}`;

export const goAuthMiddleware = `// Echo custom auth middleware
func RequireAuth(next echo.HandlerFunc) echo.HandlerFunc {
    return func(c *echo.Context) error {
        header := c.Request().Header.Get("Authorization")
        if header == "" {
            return echo.NewHTTPError(http.StatusUnauthorized, "missing token")
        }
        parts := strings.SplitN(header, " ", 2)
        if len(parts) != 2 || parts[0] != "Bearer" {
            return echo.NewHTTPError(http.StatusUnauthorized, "bad token format")
        }
        claims, err := verifyToken(parts[1])
        if err != nil {
            return echo.NewHTTPError(http.StatusUnauthorized, "invalid token")
        }
        // Store claims in context for downstream handlers
        c.Set("user", claims)
        return next(c)
    }
}`;

<TsGo ts={tsAuthMiddleware} go={goAuthMiddleware} />

<GoOnly title="การเก็บค่าใน context">

`c.Set(key, value)` เก็บค่าอะไรก็ได้ลงใน context ที่ scoped ตาม request ดึงกลับด้วย `c.Get(key)` แล้ว type assert เอง หรือใช้ generic `echo.ContextGet` ที่คืน error แทนการ panic เมื่อ type ไม่ตรง เทียบได้กับ `req.user = claims` ใน Express:

```go
claims := c.Get("user").(*MyClaims)            // panics if the type is wrong

claims, err := echo.ContextGet[*MyClaims](c, "user")   // error if missing or wrong type
```

ใช้ typed key (local `type contextKey string`) เพื่อหลีกเลี่ยงการชนกันใน codebase ขนาดใหญ่

</GoOnly>

> รันบนเครื่องของคุณ — ต้องการ Echo module และ network port

export const quizQuestionsMiddleware = [
  {
    q: 'ฟังก์ชัน Echo middleware มี signature แบบใด?',
    options: [
      '(req, res, next) => void',
      'func(c *echo.Context) error',
      'func(next echo.HandlerFunc) echo.HandlerFunc',
      'func(w http.ResponseWriter, r *http.Request)',
    ],
    answer: 2,
  },
  {
    q: 'Built-in middleware ใดที่จับ panic และคืน response 500?',
    options: ['middleware.RequestLogger()', 'middleware.Recover()', 'middleware.CORS()', 'middleware.Secure()'],
    answer: 1,
  },
  {
    q: 'Echo v5 ใช้ logging API ตัวไหนสำหรับ e.Logger และ RequestLogger()?',
    options: ['echo.Logger interface ของ Echo เอง', 'log/slog จาก standard library', 'fmt.Println', 'zap'],
    answer: 1,
  },
  {
    q: 'ใช้วิธีใดในการ apply middleware เฉพาะ route group?',
    options: [
      'e.Use() พร้อม path prefix option',
      'group.Use(middlewareFn)',
      'middleware.WithGroup(name)',
      'e.GroupMiddleware(path, fn)',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="api-echo/middleware" questions={quizQuestionsMiddleware} />

<ProgressTracker client:visible id="api-echo/middleware" />
