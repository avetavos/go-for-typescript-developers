---
title: Channels
description: เรียนรู้ channels แบบ unbuffered และ buffered สำหรับสื่อสารระหว่าง goroutines; เปรียบเทียบกับ Promises และ callbacks
---

import TsGo from '../../../../components/TsGo.astro';
import Playground from '../../../../components/Playground.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import GoOnly from '../../../../components/GoOnly.astro';

## Promises/Callbacks vs Channels

ใน JavaScript เมื่อคุณต้องการส่งผลลัพธ์จาก async operation ไปยังโค้ดอื่น คุณใช้ Promises หรือ callbacks:

```typescript
function fetchUser(id: number): Promise<User> {
  return new Promise((resolve) => {
    // ... async work ...
    resolve(user);
  });
}
const user = await fetchUser(1); // await the Promise
```

ใน Go **channels** เป็น typed pipe ระดับ first-class สำหรับส่งค่าระหว่าง goroutines channel เป็นทั้งช่องทางสื่อสารและกลไก synchronization ในตัว:

```go
ch := make(chan User)       // create a channel for User values
go func() { ch <- user }() // send into the channel
user := <-ch               // receive from the channel (blocks until a value arrives)
```

## Unbuffered vs Buffered Channels

export const tsPromiseCode = `// TypeScript: Promise for a single future value
function getResult(): Promise<number> {
  return new Promise(resolve => {
    setTimeout(() => resolve(42), 100);
  });
}
const result = await getResult();
console.log(result); // 42`;

export const goChannelCode = `// Go: unbuffered channel — sender and receiver sync up
package main

import "fmt"

func getResult(ch chan<- int) {
    ch <- 42 // send; blocks until someone receives
}

func main() {
    ch := make(chan int) // unbuffered
    go getResult(ch)
    result := <-ch // receive; blocks until sender sends
    fmt.Println(result) // 42
}`;

<TsGo ts={tsPromiseCode} go={goChannelCode} />

## Buffered Channels

Buffered channel มี queue อยู่ภายใน sender จะไม่ block จนกว่า buffer จะเต็ม และ receiver จะไม่ block ตราบใดที่ buffer ยังมีค่าอยู่ ช่วยแยก producer ออกจาก consumer:

export const tsQueueCode = `// TypeScript: simulate a buffer with an array
const queue: number[] = [];
function enqueue(val: number) {
  queue.push(val); // does not block
}
enqueue(1); enqueue(2); enqueue(3);
// must drain it yourself`;

export const goBufferedCode = `// Go: buffered channel with a queue of 3
package main

import "fmt"

func main() {
    ch := make(chan int, 3) // buffer capacity 3

    ch <- 1 // does not block (space available)
    ch <- 2
    ch <- 3
    // ch <- 4 // would block — buffer full

    fmt.Println(<-ch) // 1
    fmt.Println(<-ch) // 2
    fmt.Println(<-ch) // 3
}`;

<TsGo ts={tsQueueCode} go={goBufferedCode} />

## Range over Channels

ใช้ `for range` ไล่อ่านค่าจาก channel จนกว่าจะถูก close ฝั่ง producer จะ close channel เพื่อบอกว่าส่งครบแล้ว คล้ายกับการ return ออกจาก async generator:

export const tsIterableCode = `// TypeScript: async generator as an iterable stream
async function* generate() {
  yield 1; yield 2; yield 3;
}
for await (const val of generate()) {
  console.log(val);
}`;

export const goRangeChannelCode = `// Go: range over a channel closed by the producer
package main

import (
    "fmt"
    "sync"
)

func produce(ch chan<- int, wg *sync.WaitGroup) {
    defer wg.Done()
    for i := 1; i <= 5; i++ {
        ch <- i
    }
}

func main() {
    ch := make(chan int, 5)
    var wg sync.WaitGroup
    wg.Add(1)
    go produce(ch, &wg)
    wg.Wait()
    close(ch) // signal: no more values

    for v := range ch { // drains until channel is closed
        fmt.Println(v)
    }
}`;

<TsGo ts={tsIterableCode} go={goRangeChannelCode} />

<GoOnly title="Channel Direction — Type Safety สำหรับ Channels">

Go ให้คุณจำกัด channel เป็น send-only หรือ receive-only ได้ใน function signature ช่วยสื่อเจตนาและให้ compiler บังคับใช้ให้:

```go
func producer(ch chan<- int) { // send-only: ch <- value
    ch <- 42
}

func consumer(ch <-chan int) { // receive-only: v := <-ch
    val := <-ch
    fmt.Println(val)
}
```

ส่ง `chan int` แบบ bidirectional เข้า function ไหนก็ได้ Go จะ narrow type ให้อัตโนมัติตอนเรียก แต่ถ้าลองรับค่าข้างใน `producer` จะ compile error ทันที

</GoOnly>

export const playgroundChannelsCode = `package main

import (
	"fmt"
	"sync"
)

func producer(ch chan<- int, wg *sync.WaitGroup) {
	defer wg.Done()
	for i := 1; i <= 5; i++ {
		ch <- i
	}
}

func main() {
	ch := make(chan int, 5)
	var wg sync.WaitGroup
	wg.Add(1)
	go producer(ch, &wg)
	wg.Wait()
	close(ch)
	for v := range ch {
		fmt.Println("Received:", v)
	}
}`;

<Playground code={playgroundChannelsCode} />

export const quizQuestionsChannels = [
  { q: 'อะไรคือความแตกต่างระหว่าง unbuffered และ buffered channel?', options: ['Unbuffered เร็วกว่าเสมอ', 'Unbuffered blocks จนกว่า receiver พร้อม; buffered มี queue', 'Buffered ปลอดภัยกว่าจาก race conditions', 'ไม่มีความแตกต่าง'], answer: 1 },
  { q: 'syntax ใดที่ใช้รับค่าจาก channel?', options: ['ch.receive()', 'ch.get()', '<-ch', 'ch->'], answer: 2 },
  { q: 'ต้องทำอะไรก่อนใช้ for range บน channel?', options: ['ล้าง buffer', 'close channel', 'รอ 1 second', 'ไม่ต้องทำอะไร'], answer: 1 },
  { q: 'chan<- int หมายความว่าอะไร?', options: ['Receive-only channel', 'Buffered channel ขนาด 1', 'Send-only channel', 'Bidirectional channel'], answer: 2 },
];

<Quiz client:visible id="concurrency/channels" questions={quizQuestionsChannels} />

<ProgressTracker client:visible id="concurrency/channels" />
