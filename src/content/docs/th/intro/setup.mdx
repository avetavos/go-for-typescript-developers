---
title: ตั้งค่า Go
description: ติดตั้ง Go และเรียนรู้คำสั่ง CLI ที่จำเป็น — go run, go build, go mod init, go install — เทียบกับ node, tsc, และ npm
---

import TsGo from '../../../../components/TsGo.astro';
import Playground from '../../../../components/Playground.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import GoOnly from '../../../../components/GoOnly.astro';

## ตั้งค่า environment Go ของคุณ

การตั้งค่า TypeScript ต้องใช้ Node.js, npm (หรือ package manager อื่น) และ `tsc` ส่วน Go มี toolchain เดียวแบบรวมศูนย์ — ติดตั้ง Go ครั้งเดียวได้ครบทุกอย่าง: compiler, formatter, test runner และ module manager

## ติดตั้ง Go

ดาวน์โหลด official installer จาก [go.dev/dl](https://go.dev/dl/) หลังติดตั้ง ตรวจสอบด้วย:

```bash
go version
# go version go1.27.1 darwin/arm64
```

โดยค่าเริ่มต้น Go จะติดตั้งที่ `/usr/local/go` ส่วน `GOPATH` (workspace directory, ค่าเริ่มต้นคือ `~/go`) คือที่เก็บ module ที่ดาวน์โหลดมาและ binary ที่ติดตั้งไว้ ในทางปฏิบัติคุณแทบไม่ต้องไปยุ่งกับ `GOPATH` เลย — `go mod` จัดการให้หมด

## Go CLI vs Node/npm CLI

นักพัฒนา Node.js ทุกคนรู้จัก `node`, `npm` และ `tsc` ส่วน Go รวมทุกอย่างนี้ไว้ใน `go` command เดียว

export const tsCliCode = `# TypeScript / Node.js toolchain
node index.js            # run a file
npx ts-node src/index.ts # run TypeScript directly
tsc                      # compile TypeScript
npm install              # install dependencies
npm run build            # run a build script
npx <tool>              # run a package binary`;

export const goCliCode = `# Go toolchain (all one command)
go run main.go           # compile + run in one step
go run .                 # run the current package
go build -o myapp .      # compile to a binary
go mod init github.com/you/myapp  # init a new module
go get github.com/gin-gonic/gin   # add a dependency
go install tool@latest   # install a CLI tool globally`;

<TsGo ts={tsCliCode} go={goCliCode} />

## go run vs go build

`go run` คือ development loop ของคุณ — คอมไพล์แล้วรันในขั้นตอนเดียว เหมือน `ts-node` หรือ `npx tsx` ที่ใช้ตอน develop ส่วน `go build` สร้าง binary แบบ standalone ที่เอาไป deploy ได้ Go ไม่มี "watch mode" ในตัว แต่เครื่องมืออย่าง [air](https://github.com/air-verse/air) ช่วยทำ live reload ได้

export const tsRunCode = `# TypeScript development loop
npx ts-node src/index.ts    # compile + run (dev)
tsc && node dist/index.js   # compile then run (CI/prod)
npx tsx watch src/index.ts  # watch mode`;

export const goRunCode = `# Go development loop
go run .                    # compile + run (dev)
go build -o ./bin/app . && ./bin/app  # compile then run
# air                       # watch mode (third-party)`;

<TsGo ts={tsRunCode} go={goRunCode} />

## go mod init — เทียบเท่ากับ npm init

เมื่อเริ่มโปรเจกต์ Go ใหม่ ให้ initialize module ด้วย `go mod init` คำสั่งนี้จะสร้างไฟล์ `go.mod` — เทียบเท่ากับ `package.json` ของ Go

export const tsNpmInitCode = `# TypeScript
npm init -y
# creates package.json with name, version, deps

# then add a dependency:
npm install express
# updates package.json + creates package-lock.json`;

export const goModInitCode = `# Go
go mod init github.com/yourname/myapp
# creates go.mod

# then add a dependency:
go get github.com/gin-gonic/gin
# updates go.mod + creates go.sum (like package-lock.json)`;

<TsGo ts={tsNpmInitCode} go={goModInitCode} />

## go install — CLI tool แบบ global

`npm install -g` ใช้ติดตั้ง CLI tool แบบ global ส่วน `go install` ก็ทำแบบเดียวกัน — คอมไพล์ module แล้ววาง binary ไว้ใน `$GOPATH/bin` (อย่าลืมเพิ่ม path นี้ใน `PATH` ของคุณ)

export const tsInstallCode = `# TypeScript: global CLI tools
npm install -g typescript
npm install -g prettier
npx create-react-app myapp`;

export const goInstallCode = `# Go: global CLI tools
go install golang.org/x/tools/gopls@latest      # LSP server
go install github.com/air-verse/air@latest       # live reload
go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest`;

<TsGo ts={tsInstallCode} go={goInstallCode} />

<GoOnly title="GOPATH, GOROOT, และ module cache">

คุณจะเห็นการอ้างถึง environment variable เหล่านี้อยู่เป็นระยะ:

| Variable | ค่าเริ่มต้น | คืออะไร |
|----------|------------|---------|
| `GOROOT` | `/usr/local/go` | ตำแหน่งที่ติดตั้ง Go toolchain |
| `GOPATH` | `~/go` | workspace ของคุณ — module cache อยู่ที่ `~/go/pkg/mod/` |
| `GOBIN` | `$GOPATH/bin` | ตำแหน่งที่ `go install` วาง binary |

รัน `go env` เพื่อดู environment variable ของ Go ทั้งหมด หรือ `go env GOPATH` เพื่อดูทีละค่า คุณแทบไม่ต้องแก้ค่าพวกนี้เลย — `go mod` ทำให้ module cache โปร่งใสอยู่แล้ว

```bash
# useful diagnostics
go env          # print all Go environment variables
go env GOPATH   # print just GOPATH
go version      # print installed Go version
go doc fmt      # read the fmt package docs in the terminal
```

</GoOnly>

## Playground

บทนี้เป็นบท setup — playground จึงไม่ค่อยมีบทบาทตรงนี้ (ไม่มีอะไรต้องติดตั้งใน browser) แต่ในบทถัดไป (Hello World) คุณจะได้ใช้ playground รันโปรแกรม Go แรกแบบเต็มๆ

export const quizQuestionsSetup = [
  { q: 'Go เทียบเท่ากับ `ts-node src/index.ts` คืออะไร?', options: ['go install .', 'go build .', 'go run .', 'go test .'], answer: 2 },
  { q: '`go mod init` สร้างไฟล์อะไร?', options: ['go.json', 'package.go', 'go.mod', 'go.sum'], answer: 2 },
  { q: '`go install` วาง compiled binary ไว้ที่ไหน?', options: ['./bin/', '/usr/local/bin/', '$GOPATH/bin', './node_modules/.bin/'], answer: 2 },
  { q: 'go.sum เทียบเท่ากับอะไรใน Node.js ecosystem?', options: ['package.json', '.npmrc', 'package-lock.json', '.yarnrc'], answer: 2 },
];

<Quiz client:visible id="intro/setup" questions={quizQuestionsSetup} />

<ProgressTracker client:visible id="intro/setup" />
